# Emo

![](screenshot.png)

Shows you pixelated emojis in the terminal!

## Setup

You need to download the [SerenityOS emojis](https://emoji.serenityos.org/) and unzip the file into this project's directory. Then you can run `make` and run the executable that it spits out by running `./emo`. Running it currently only works from the project directory.
